# Copy this to a file named .env (same folder as the script) and fill in as needed.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=your_database
MONGODB_COLLECTION=your_collection

# Optional Mongo filters (JSON)
# MONGODB_QUERY={"status":"active"}
# MONGODB_PROJECTION={"_id":1,"title":1,"published_at":1}

# Output locations
DUCKDB_FILE=scraped.duckdb
DUCKDB_TABLE=scraped
OUT_PARQUET=export/export.parquet

# If you prefer a Parquet folder with multiple files, set append and point OUT_PARQUET to a folder:
# OUT_PARQUET=export
# BATCH_SIZE=5000
# EXPLODE_FIELD=items         # optional: name of array field to explode into a child table
# EXPLODE_TABLE=scraped_items # child table name